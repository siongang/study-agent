"""Extract topic coverage from syllabus/outline documents."""
from pathlib import Path
from typing import Any


def extract_coverage(path: str | Path, text: str = "") -> list[dict[str, Any]]:
    """Parse syllabus/outline text into list of topics. Stub returns empty list."""
    return []
